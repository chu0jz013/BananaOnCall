# Exercises

Start at `ex01` and move sequentially. The isolated exercises intentionally use tiny packages; copy each folder elsewhere or add your own `_test.go` while solving. From Phase 4 onward, work directly in `backend/` and `frontend/` following `CHECKLIST.md`.
