# Google Calendar

**Rule:** bananaoncall owns schedule truth; Calendar gets generated shifts.

Exercises:
- Model `Rotation`, `Shift`, and `Override` first.
- Decide OAuth user consent vs workspace service account/delegation.
- Create/update/delete projected shift events.
- Store external event ID and sync hash.
- Reconcile drift periodically.
- Handle timezone explicitly.

Suggested event summary: `[Primary] Platform — Nam`
