# Reference solutions

Do not browse this directory during the first attempt. These are intentionally concise references, not the only correct implementations. Later phases are solved by evolving the real application instead of copying snippets.
